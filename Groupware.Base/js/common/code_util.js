var newCode = function (tree_code, tree_level, tree_key, tree_value, tree_order) {

	var result = "";

	var new_tree_code = "";
	var new_tree_level = parseInt($('#tree_level').val()) + 1;
	var new_tree_key = "";
	var new_tree_value = "";
	var new_tree_order = "";

}